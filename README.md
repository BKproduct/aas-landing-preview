# aas-landing-preview

Public **GitHub Pages** preview of the AI-Automation.Studio one-page marketing landing (static HTML in `docs/`).

**Live site:** after Pages is enabled, open  
`https://bkproduct.github.io/aas-landing-preview/`

**Source of truth for edits:** sync from  
`PROJECTS/AI-Automation/aas-landing/github-pages-bundle/`  
in the main workspace repo, then replace `docs/` here and push.

Booking buttons use Calendly via `data-booking-url` on the `<html>` element in `docs/index.html`.
